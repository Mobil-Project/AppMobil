import 'package:flutter/material.dart';
class Regles extends StatelessWidget {
  const Regles({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar(title:const Text('Regles page ')),
      body: const Center(
        child: Text('Regles page '),

      ),

    );
  }
}
